apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-notification
  template:
    metadata:
      labels:
        app: db-notification
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_DB
              value: cd_notification
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: password
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: db-notification
spec:
  type: NodePort
  selector:
    app: db-notification
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30037