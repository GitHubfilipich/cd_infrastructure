apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-generator
  template:
    metadata:
      labels:
        app: db-generator
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_DB
              value: cd_generator
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: password
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: db-generator
spec:
  type: NodePort
  selector:
    app: db-generator
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30034